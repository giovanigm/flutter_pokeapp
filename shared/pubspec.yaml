name: shared
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.3
  hive_flutter: ^1.1.0
  get_it: ^7.2.0

  pokemon_api:
    path: ../libraries/pokemon_api

  design_system:
    path: ../libraries/design_system

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.1.11
  hive_generator: ^1.1.3


flutter:
  uses-material-design: true
